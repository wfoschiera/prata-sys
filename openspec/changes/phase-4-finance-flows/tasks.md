## 1. Models

- [x] 1.1 Add `TipoTransacao` enum to `backend/app/models.py` with values: `receita`, `despesa`
- [x] 1.2 Add `CategoriaTransacao` enum with all 12 categories: `SERVICO`, `VENDA_EQUIPAMENTO`, `RENDIMENTO`, `CAPITAL_FIXO`, `COMBUSTIVEL`, `MANUTENCAO_EQUIPAMENTO`, `MANUTENCAO_VEICULO`, `MANUTENCAO_ESCRITORIO`, `COMPRA_MATERIAL`, `MO_CLT`, `MO_DIARISTA`, `ADMIN`
- [x] 1.3 Define `INCOME_CATEGORIES: frozenset[CategoriaTransacao]` and `EXPENSE_CATEGORIES: frozenset[CategoriaTransacao]` as module-level constants in `models.py`
- [x] 1.4 Add `Transacao` SQLModel table with columns: `id` (UUID PK), `tipo` (TipoTransacao, NOT NULL), `categoria` (CategoriaTransacao, NOT NULL), `valor` (sa.Numeric(12,2), NOT NULL), `data_competencia` (Date, NOT NULL), `descricao` (Text, nullable), `nome_contraparte` (varchar(200), nullable — free-text fallback when counterpart is not a registered fornecedor), `fornecedor_id` (UUID FK → fornecedor.id, SET NULL, nullable, indexed — populated after Phase 6), `service_id` (UUID FK → service.id, SET NULL, nullable, indexed), `client_id` (UUID FK → client.id, SET NULL, nullable, indexed), `created_at` (DateTime, NOT NULL), `updated_at` (DateTime, NOT NULL)
  - Note: `fornecedor_id` FK will reference a table that doesn't exist yet (Phase 6). Add the column as nullable with no FK constraint for now; Phase 6 will add the FK constraint via a separate migration.
- [x] 1.5 Add `TransacaoCreate` Pydantic schema with a `model_validator(mode="after")` that:
  - rejects `valor <= 0`
  - rejects `categoria` not in the expected set for `tipo`
  - rejects `client_id` when `tipo=despesa`
- [x] 1.6 Add `TransacaoUpdate` schema (all fields optional, omits `tipo` and `client_id`) with a `model_validator(mode="after")` that validates categoria/tipo compatibility when both are present
- [x] 1.7 Add `TransacaoPublic` schema with embedded `service: ServiceSummary | None` and `client: ClientSummary | None` (reuse or create minimal summary schemas)
- [x] 1.8 Add `ResumoMensal` schema: `ano: int`, `mes: int`, `total_receitas: Decimal`, `total_despesas: Decimal`, `resultado_liquido: Decimal`
- [x] 1.9 Add `TransacoesPublic` schema (list wrapper with `data: list[TransacaoPublic]` and `count: int`)

## 2. Alembic Migration

- [x] 2.1 Run `alembic revision --autogenerate -m "add transacao table"` from `backend/`
- [x] 2.2 Review the generated migration: verify `sa.Numeric(12, 2)` for `valor`, both FK constraints with `ondelete="SET NULL"`, indexes on `service_id`, `client_id`, and `data_competencia`
- [x] 2.3 Add an explicit index on `data_competencia` in the migration if not autogenerated (needed for monthly resumo queries)
- [x] 2.4 Run `alembic upgrade head` in dev and confirm the table is created correctly

## 3. Permissions Update

- [x] 3.1 Add `"manage_financeiro": "Gerenciar Financeiro"` and `"view_financeiro": "Visualizar Financeiro"` to `ALL_PERMISSIONS` in `backend/app/core/permissions.py`
- [x] 3.2 Update `ROLE_PERMISSIONS` in `permissions.py`:
  - `finance`: add `manage_financeiro`, `view_financeiro` (existing `view_contas_pagar` and `view_contas_receber` remain)
  - `admin`: add `view_financeiro`
- [x] 3.3 Write unit tests: verify `finance` role includes all 4 finance permissions; verify `admin` includes `view_financeiro`; verify `client` has none

## 4. CRUD

- [x] 4.1 Add `create_transacao(session, transacao_in: TransacaoCreate) -> Transacao` to `crud.py` — validate service and client existence if FKs provided; raise `HTTPException(404)` if not found
- [x] 4.2 Add `get_transacao(session, transacao_id: UUID) -> Transacao | None`
- [x] 4.3 Add `get_transacoes(session, *, tipo, categoria, data_inicio, data_fim, service_id, skip, limit) -> tuple[list[Transacao], int]` — use `selectinload(Transacao.service)` and `selectinload(Transacao.client)` to prevent N+1; apply filters conditionally
- [x] 4.4 Add `update_transacao(session, transacao: Transacao, transacao_in: TransacaoUpdate) -> Transacao` — apply partial update; re-validate service FK if `service_id` changes
- [x] 4.5 Add `delete_transacao(session, transacao: Transacao) -> None`
- [x] 4.6 Add `get_resumo_mensal(session, ano: int, mes: int) -> ResumoMensal` — single query using `func.sum(Transacao.valor)` grouped by `tipo`, filtered by `data_competencia` within the month; return zero-valued schema if no rows
- [x] 4.7 Write unit tests for all CRUD functions: create with valid data, create with invalid FK (404), list with each filter, list with combined filters, update, delete, resumo with data, resumo with no data (zeros)

## 5. API Routes

- [x] 5.1 Create `backend/app/api/routes/transacoes.py` with router prefix `/transacoes`
- [x] 5.2 `GET /transacoes` — query params: `tipo`, `categoria`, `data_inicio` (date), `data_fim` (date), `service_id` (UUID), `skip` (int, default 0), `limit` (int, default 100); permission: `view_financeiro`; use `get_transacoes` CRUD; return `TransacoesPublic`
- [x] 5.3 `POST /transacoes` — body: `TransacaoCreate`; permission: `manage_financeiro`; return `TransacaoPublic`, status 201
- [x] 5.4 `GET /transacoes/resumo` — query params: `ano` (int, default current year), `mes` (int 1–12, default current month); permission: `view_financeiro`; return `ResumoMensal`
- [x] 5.5 `GET /transacoes/{id}` — permission: `view_financeiro`; return `TransacaoPublic`; 404 if not found
- [x] 5.6 `PATCH /transacoes/{id}` — body: `TransacaoUpdate`; permission: `manage_financeiro`; return `TransacaoPublic`; 404 if not found
- [x] 5.7 `DELETE /transacoes/{id}` — permission: `manage_financeiro`; return HTTP 204; 404 if not found
- [x] 5.8 Register the `transacoes` router in `backend/app/api/main.py`
- [x] 5.9 Write API tests (`backend/tests/api/routes/test_transacoes.py`):
  - POST: valid expense, valid income with service, invalid valor (0), categoria/tipo mismatch (422), client_id on despesa (422), non-existent service_id (404), unauthorized (403)
  - GET list: all transactions, filter by tipo, filter by categoria, filter by date range, combined filters, empty result
  - GET resumo: month with data, month with no data, default to current month
  - GET by id: found, not found (404)
  - PATCH: valid update, update service_id, 404
  - DELETE: success (204), not found (404)
  - Permission guards: `view_financeiro` required for GETs, `manage_financeiro` required for writes; superuser bypass

## 6. Regenerate API Client

- [x] 6.1 Run `bash ./scripts/generate-client.sh` after the backend is running with the new endpoints
- [x] 6.2 Verify that `TransacaoCreate`, `TransacaoPublic`, `TransacoesPublic`, `ResumoMensal`, `TipoTransacao`, `CategoriaTransacao` are present in `frontend/src/client/`

## 7. Frontend: Finance Dashboard (`/financeiro`)

- [x] 7.1 Create route file `frontend/src/routes/_layout/financeiro/index.tsx`
- [x] 7.2 Add `beforeLoad` guard: redirect to `/` if user lacks `view_financeiro` permission
- [x] 7.3 Compute the last 6 month/year pairs (including current month) in a utility function
- [x] 7.4 Fetch all 6 monthly resumos in parallel using `Promise.all` with TanStack Query (`useQueries` or 6 independent `useQuery` calls)
- [x] 7.5 Render three KPI cards for the current month using shadcn/ui `Card`:
  - "Receitas" — `total_receitas` formatted as BRL currency
  - "Despesas" — `total_despesas` formatted as BRL currency
  - "Resultado Líquido" — `resultado_liquido` (green if positive, red if negative)
- [x] 7.6 Render a bar chart (use recharts, already available via shadcn/ui charts) with grouped bars for receitas and despesas per month; x-axis labels: "Jan", "Fev", etc.
- [x] 7.7 Show loading skeleton while queries are in-flight
- [x] 7.8 Show error toast if any monthly resumo request fails

## 8. Frontend: Transactions List (`/financeiro/transacoes`)

- [x] 8.1 Create route file `frontend/src/routes/_layout/financeiro/transacoes.tsx`
- [x] 8.2 Add `beforeLoad` guard: redirect to `/` if user lacks `view_financeiro`
- [x] 8.3 Add filter bar with: `tipo` select (Todos | Receita | Despesa), `categoria` select (all categories, filtered by selected tipo if applicable), date range pickers for `data_inicio` and `data_fim`, "Limpar filtros" button
- [x] 8.4 Fetch transactions via `GET /api/v1/transacoes` passing active filter values as query params; use TanStack Query with filter values as query key
- [x] 8.5 Render a table with columns: Data (formatted `dd/MM/yyyy`), Tipo (badge: verde for receita, vermelho for despesa), Categoria (PT-BR label), Descrição, Contraparte, Valor (BRL formatted, right-aligned), Serviço (link to `/services/{id}` if present), Ações (edit, delete)
- [x] 8.6 "Nova Transação" button visible only to users with `manage_financeiro`; opens Add Transaction modal
- [x] 8.7 Delete confirmation dialog: "Tem certeza que deseja excluir esta transação? Esta ação não pode ser desfeita."
- [x] 8.8 Show empty-state message when no transactions match filters: "Nenhuma transação encontrada"
- [x] 8.9 Show loading skeleton while fetching

## 9. Frontend: Add Transaction Form

- [x] 9.1 Create `frontend/src/components/Financeiro/AddTransacao.tsx` as a Dialog/Sheet modal
- [x] 9.2 Define Zod schema with all fields; use `z.number()` for `valor` with `onChange={(e) => field.onChange(e.target.valueAsNumber)}` (not `z.coerce.number()`); use `z.enum()` with `error:` for enums (not `required_error:`)
- [x] 9.3 `tipo` select is the first field; changing it resets `categoria` to undefined
- [x] 9.4 `categoria` select dynamically shows only categories valid for the selected `tipo` (filter the enum options client-side)
- [x] 9.5 `valor` number input with `step="0.01"` and `min="0.01"`; label shows "Valor (R$)"
- [x] 9.6 `data_competencia` date picker (native `<input type="date">` or a shadcn/ui DatePicker); defaults to today
- [x] 9.7 `service_id` combobox (searchable select of existing services) — optional
- [x] 9.8 `client_id` combobox — only rendered when `tipo=receita`; hidden (not disabled) for `tipo=despesa`
- [x] 9.9 `nome_contraparte` text input — optional; label: "Nome da contraparte (fornecedor, colaborador…)"
- [x] 9.10 `descricao` textarea — optional; label: "Descrição / Memo"
- [x] 9.11 On submit: call `POST /api/v1/transacoes`; on success close modal, invalidate transactions query, show success toast "Transação registrada com sucesso"
- [x] 9.12 On API error: show error toast with the error detail message

## 10. Frontend: Edit Transaction Form

- [x] 10.1 Create `frontend/src/components/Financeiro/EditTransacao.tsx` using the same form structure as Add, but `tipo` field is read-only (displayed as a static badge, not a select)
- [x] 10.2 Pre-populate all form fields from the existing `TransacaoPublic` object
- [x] 10.3 On submit: call `PATCH /api/v1/transacoes/{id}`; on success close modal, invalidate transactions query, show success toast

## 11. Frontend: Contas a Pagar (`/financeiro/contas-a-pagar`)

- [x] 11.1 Create route file `frontend/src/routes/_layout/financeiro/contas-a-pagar.tsx`
- [x] 11.2 Add `beforeLoad` guard: redirect to `/` if user lacks `view_contas_pagar`
- [x] 11.3 Render the same transaction table as the Transactions list, pre-filtered to `tipo=despesa`; disable the tipo filter (locked to Despesa)
- [x] 11.4 Page title: "Despesas" (not "Contas a Pagar" to avoid implying due-date workflow that doesn't exist yet); add a sub-label: "Histórico de despesas registradas"
- [x] 11.5 "Nova Despesa" button (shortcut: opens Add Transaction modal with `tipo=despesa` pre-selected and locked)

## 12. Frontend: Contas a Receber (`/financeiro/contas-a-receber`)

- [x] 12.1 Create route file `frontend/src/routes/_layout/financeiro/contas-a-receber.tsx`
- [x] 12.2 Add `beforeLoad` guard: redirect to `/` if user lacks `view_contas_receber`
- [x] 12.3 Render the transaction table pre-filtered to `tipo=receita`; disable the tipo filter
- [x] 12.4 Page title: "Receitas"; sub-label: "Histórico de receitas registradas"
- [x] 12.5 "Nova Receita" button (opens Add Transaction modal with `tipo=receita` pre-selected and locked)

## 13. Frontend: Sidebar Update

- [x] 13.1 Add a "Financeiro" section to the sidebar (below Serviços, above Admin section)
- [x] 13.2 Render sidebar links conditionally:
  - "Dashboard Financeiro" → `/financeiro` — visible when `view_financeiro`
  - "Transações" → `/financeiro/transacoes` — visible when `view_financeiro`
  - "Despesas" → `/financeiro/contas-a-pagar` — visible when `view_contas_pagar`
  - "Receitas" → `/financeiro/contas-a-receber` — visible when `view_contas_receber`
- [x] 13.3 Superusers see all finance links
- [x] 13.4 Hidden links are fully hidden (not grayed out)

## 14. Verification

- [x] 14.1 Run backend tests: `bash scripts/test.sh` — all pass; check that `test_transacoes.py` covers all scenarios in the spec
- [x] 14.2 Run frontend lint + build: `cd frontend && bun run lint && bun run build`
- [x] 14.3 E2E test (Playwright): finance user can create a receita linked to a service order; the transaction appears in the list
- [x] 14.4 E2E test (Playwright): finance user creates a despesa with `categoria=COMBUSTIVEL`; the transaction appears under Despesas
- [x] 14.5 E2E test (Playwright): attempt to create a despesa with `categoria=SERVICO` — API returns 422
- [x] 14.6 E2E test (Playwright): Finance Dashboard KPI cards show correct totals for the current month
- [x] 14.7 E2E test (Playwright): 6-month bar chart renders with correct monthly labels and grouped bars
- [x] 14.8 E2E test (Playwright): admin user (with `view_financeiro`) can view but not create transactions (no "Nova Transação" button visible)
- [x] 14.9 E2E test (Playwright): client user sees no finance sidebar links
- [x] 14.10 E2E test (Playwright): delete a service that has linked transactions — verify transactions remain with `service_id=null`
